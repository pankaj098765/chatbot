# ─────────────────────────────────────────────────────────────────
# REQUIRED — Bot & Infrastructure
# ─────────────────────────────────────────────────────────────────

# Telegram Bot Token — get one from @BotFather
BOT_TOKEN=your_telegram_bot_token_here

# MongoDB connection string
MONGODB_URI=mongodb://mongo:27017

# Redis connection string
REDIS_URL=redis://redis:6379

# MongoDB database name
DB_NAME=anonymous_chat

# ─────────────────────────────────────────────────────────────────
# BRANDING — White-label identity
# ─────────────────────────────────────────────────────────────────

# Your bot's display name (shown in welcome messages)
# This is the default — change it to your own brand name
BRAND_NAME=Anonymous Chat

# ─────────────────────────────────────────────────────────────────
# LANGUAGE — Default user experience
# ─────────────────────────────────────────────────────────────────

# Default UI language for new users (ISO 639-1 code)
# Options: en, hi, es, fr, de, pt, ar, ru, tr, id
DEFAULT_LANGUAGE=en

# Comma-separated list of languages users can choose from
# Must be a subset of the supported list above
ALLOWED_LANGUAGES=en,hi,es

# Default chat mode for new users
# Options: mixed | english | native
#   mixed   — user's language + English blend
#   english — English only regardless of user language
#   native  — user's chosen language only
DEFAULT_CHAT_MODE=mixed

# ─────────────────────────────────────────────────────────────────
# FEATURE TOGGLES
# ─────────────────────────────────────────────────────────────────

# Enable LLM-powered (AI) fallback responses (requires OPENAI_API_KEY)
# Set to false to use template-only responses — no API key needed
AI_ENABLED=true

# Enable Telegram Stars payment commands (/pay, /vip)
# Set to false to hide payment features entirely
PAYMENT_ENABLED=true

# ─────────────────────────────────────────────────────────────────
# LLM / AI (only required when AI_ENABLED=true)
# ─────────────────────────────────────────────────────────────────

# Which AI provider to use.
# Supported values: openai | gemini | grok | groq | mistral | deepseek | together | anthropic | custom
# Default: auto-detected from whichever provider-specific key is set below.
# You can leave this blank when using a provider-specific key (e.g. GEMINI_API_KEY).
LLM_PROVIDER=

# Generic API key — works with any provider listed above.
# You can use this OR the provider-specific key below (not both needed).
LLM_API_KEY=

# ── Provider-specific API key aliases ─────────────────────────────
# Set ONLY the key that matches your AI provider.
# The bot automatically detects the provider from whichever key is present.
# You do NOT need to set LLM_PROVIDER or LLM_API_KEY when using these.
#
OPENAI_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
GROQ_API_KEY=
GROK_API_KEY=
MISTRAL_API_KEY=
DEEPSEEK_API_KEY=
TOGETHER_API_KEY=

# Model name — must match the provider's model naming convention.
# Examples:
#   openai   → gpt-4o-mini, gpt-4o, gpt-3.5-turbo
#   gemini   → gemini-1.5-flash, gemini-1.5-pro
#   grok     → grok-3-mini, grok-2
#   groq     → llama-3.3-70b-versatile, mixtral-8x7b-32768
#   mistral  → mistral-small-latest, mistral-large-latest
#   deepseek → deepseek-chat
#   together → mistralai/Mistral-7B-Instruct-v0.1
#   anthropic → claude-3-haiku-20240307, claude-3-5-sonnet-20241022
#
# OPTIONAL — if left blank the code picks a sensible default for each provider.
# Only set this if you want to use a specific model other than the default.
LLM_MODEL=

# (Optional) Custom REST endpoint — only needed for LLM_PROVIDER=custom
# or to override the default endpoint of any built-in provider.
# Example (local Ollama): LLM_BASE_URL=http://localhost:11434/v1
LLM_BASE_URL=

# ─────────────────────────────────────────────────────────────────
# ADMIN DASHBOARD
# ─────────────────────────────────────────────────────────────────

# Admin dashboard bearer token — change this before deploying!
ADMIN_TOKEN=change_me_to_a_strong_secret_token

# ─────────────────────────────────────────────────────────────────
# DEBUG (development only)
# ─────────────────────────────────────────────────────────────────

DEBUG=false
